# offer-operator

## Branch status

The `work` branch currently contains only repository metadata (this README and
the Apache 2.0 license text). An operator implementation has not yet been
added, so the branch is effectively empty aside from these support files.